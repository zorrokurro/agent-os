import type { Councillor, Deliberation } from '../../types/council'

export type { Councillor, Deliberation }

export type PageView = 'list' | 'new' | 'detail'

export { DEFAULT_COUNCILLORS, DEFAULT_CHAIRMAN, MODE_DESCRIPTIONS, generateId } from '../../types/council'
